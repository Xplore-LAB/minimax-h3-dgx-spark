# MiniMax H3 第一条视频实战教程

> **版本**：2026-08-04 实战验证（spark-44a8，GB10 / 121GB 统一内存全流程跑通）
> **前置**：部署已完成（`docker ps` 里 `minimax-h3-comfyui` 为 Up，`curl http://127.0.0.1:8188/system_stats` 返回 200）
> **全程不用开浏览器**：HTTP API 提交 → 查进度 → 拿 mp4 + flac → ffmpeg 合成带声音成片

---

## 0. 三秒前置检查

```bash
docker ps | grep minimax-h3-comfyui          # Up
curl -s http://127.0.0.1:8188/system_stats | head -c 60   # {"system": ...
ls /mnt/minimax-h3/models/diffusion_models/  # 4 个模型文件在
```

---

## 1. 提交第一条视频（文生视频，带声音）

把下面脚本存成 `first_video.py`，**直接跑**（只需要 Python 标准库）：

```python
import json, urllib.request

workflow = {
 "1": {"class_type": "UNETLoader", "inputs": {"unet_name": "minimax_h3_fl2va_pruned_int8_convrot.safetensors", "weight_dtype": "default"}},
 "2": {"class_type": "CLIPLoader", "inputs": {"clip_name": "qwen3vl_32b_minimax_h3_nvfp4_awq.safetensors", "type": "minimax"}},
 "3": {"class_type": "VAELoader", "inputs": {"vae_name": "minimax_h3_video_vae_fp16.safetensors"}},
 "4": {"class_type": "VAELoader", "inputs": {"vae_name": "minimax_h3_audio_vae_fp32.safetensors"}},
 "5": {"class_type": "EmptyMiniMaxH3LatentAV", "inputs": {"width": 608, "height": 352, "length": 96, "batch_size": 1}},
 "6": {"class_type": "MiniMaxH3SigmaShift", "inputs": {"model": ["1", 0], "shift_video": 12.0, "shift_audio": 3.0}},
 "7": {"class_type": "MiniMaxH3ImageToVideo", "inputs": {"clip": ["2", 0], "vae": ["3", 0],
       "prompt": "A majestic whale swimming in the deep blue ocean, sunlight rays piercing the water surface, cinematic, stereo sound of ocean waves",
       "width": 608, "height": 352, "length": 96}},
 "8": {"class_type": "CLIPTextEncode", "inputs": {"clip": ["2", 0], "text": "blurry, low quality, deformed, camera shake"}},
 "9": {"class_type": "KSampler", "inputs": {"model": ["6", 0], "positive": ["7", 0], "negative": ["8", 0],
       "latent_image": ["7", 1], "seed": 42, "steps": 20, "cfg": 1.0,
       "sampler_name": "euler", "scheduler": "simple", "denoise": 1.0}},
 "10": {"class_type": "VAEDecode", "inputs": {"samples": ["9", 0], "vae": ["3", 0]}},
 "11": {"class_type": "VAEDecodeAudio", "inputs": {"samples": ["9", 0], "vae": ["4", 0]}},
 "12": {"class_type": "CreateVideo", "inputs": {"images": ["10", 0], "fps": 24.0}},
 "13": {"class_type": "SaveVideo", "inputs": {"video": ["12", 0], "filename_prefix": "h3_first", "format": "auto", "codec": "auto"}},
 "14": {"class_type": "SaveAudio", "inputs": {"audio": ["11", 0], "filename_prefix": "audio/h3_first"}}
}

req = urllib.request.Request("http://127.0.0.1:8188/prompt",
    data=json.dumps({"prompt": workflow, "client_id": "first"}).encode(),
    headers={"Content-Type": "application/json"})
print(json.load(urllib.request.urlopen(req, timeout=20)))  # 打印 prompt_id
```

参数说明：
- `length`: 96≈4s；124≈5s；上限 362≈15s（帧率 24fps）
- 分辨率：第一条先用 **608×352 冒烟**（实测 ~97s），再上 **864×480**（实测 ~4.3min）
- `cfg=1.0` + `euler/simple` 是 H3 官方推荐配置，别乱改

---

## 2. 查进度

```bash
curl -s http://127.0.0.1:8188/queue | python3 -m json.tool   # 队列里还有几个
docker logs -f minimax-h3-comfyui                             # tqdm 进度条实时滚动
```

生成中日志长这样（每步 ~26–47s）：

```
Requested to load MiniMaxH3 ... 19995MB Staged
  20%|██        | 4/20 [01:55<07:40, 28.8s/it]
```

---

## 3. 拿成品

生成完自动落盘到外部 NVMe：

```bash
ls -la /mnt/minimax-h3/output/h3_first_00001_.mp4      # 视频（无声 h264）
ls -la /mnt/minimax-h3/output/audio/h3_first_00001.flac # 音效（电平极低，需归一化）
```

也可用 history API 确认任务完成：

```bash
curl -s http://127.0.0.1:8188/history/<prompt_id> | python3 -m json.tool
```

---

## 4. 合成带声音成片（必须 loudnorm）

> H3 音频 VAE 输出的音效峰值约 -34dB，人耳几乎听不见；直接 mux 会"看起来有声音、听起来没有"。
> 用容器内 ffmpeg（宿主没装也行），音量归一化后峰值约 -3~-5dB。

```bash
docker exec minimax-h3-comfyui ffmpeg -y \
  -i output/h3_first_00001_.mp4 -i output/audio/h3_first_00001.flac \
  -af "loudnorm=I=-16:TP=-1.5:LRA=11" -c:v copy -c:a aac -shortest \
  output/h3_first_final.mp4

# 验证音轨
docker exec minimax-h3-comfyui ffprobe -v error -show_entries stream=codec_type -of csv output/h3_first_final.mp4
# 期望输出两行: video / audio
```

---

## 5. GIF 预览（发微信用）

```bash
docker exec minimax-h3-comfyui ffmpeg -y -i output/h3_first_final.mp4 \
  -vf "fps=8,scale=432:-1:flags=lanczos,split[s0][s1];[s0]palettegen[p];[s1][p]paletteuse" \
  -loop 0 output/h3_first_preview.gif
```

---

## 6. 常见问题

| 症状 | 原因 / 处理 |
|---|---|
| 返回 `{"error": ...}` | 模型文件路径不对 → `ls /mnt/minimax-h3/models/**/` 核对 4 个文件名 |
| 视频没声音 | 少了音频三件套（VAELoader fp32 / VAEDecodeAudio / SaveAudio）→ 照抄节点 4/11/14 |
| 生成很慢（10min+） | 分辨率/帧数太大；GB10 上 864×480×5s 约 4.3min 属正常 |
| 退出码 137 / 系统卡 | 统一内存被打满 → 先停其他大模型容器（qwen/vllm），或降分辨率 |
| 图生视频 | 参考图放 `/mnt/minimax-h3/input/`，加 `LoadImage` 节点并把输出接节点 7 的 `first_frame` |

---

## 7. 下一步

- 多条一起提交：循环调 `/prompt` 自动排队串行，不会 OOM
- 多段拼接大片 / 角色连贯性技巧 → 见 `2026-08-04_MiniMaxH3_多段视频创作教程.md`
- 服务安全：ComfyUI 默认只绑 127.0.0.1，通过 SSH 隧道或可信局域网访问
